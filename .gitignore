# Reference mirrors — cloned on demand, never committed.
# They are upstream code (openai/codex-plugin-cc, NousResearch/hermes-agent);
# we don't redistribute them. Bootstrap with:
#   .claude/skills/pull-reflect/scripts/check-drift.sh --bootstrap
ref/*
!ref/README.md

# Python
__pycache__/
*.pyc
.venv/

# Node
node_modules/

# OS
.DS_Store
Thumbs.db